# Archived code

Retired OpModes, legacy subsystems, and RR tuning tools. **Do not import from active `robot/` or `opmodes/` code.**

Moved during CursorBasicRewrite to reduce Driver Station clutter and remove Road Runner dependencies.
